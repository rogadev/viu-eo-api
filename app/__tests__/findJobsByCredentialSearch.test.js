const { describe, expect, test } = require('jest')

const findJobsByCredentialSearch = require('../lib/findJobsByCredentialSearch.js')

// describe('findJobsByCredentialSearch', () => {
//   test('findJobsByCredentialSearch()', () => {
//     const result = findJobsByCredentialSearch({
//       credential: ['degree'],
//       search: ['computer science'],
//     })
//     expect(result).toEqual({
//       jobs: [],
//       groups: [],
//     })
//   }),
//     30000
// })

// Mothball... May come back to this later.
